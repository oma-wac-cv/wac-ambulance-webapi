kind: Service
apiVersion: v1
metadata:
  name: oma-ambulance-webapi
spec:
  selector:
    pod: oma-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port
